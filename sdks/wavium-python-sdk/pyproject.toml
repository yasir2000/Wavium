[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "wavium-python-sdk"
version = "0.1.0"
description = "Wavium Python SDK scaffold"
requires-python = ">=3.10"
