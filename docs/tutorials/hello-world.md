# Hello World

A Wavium hello-world example should demonstrate the smallest portable component that can be built and executed under the runtime.

The key lesson is that the component is the unit of execution, not a native process.