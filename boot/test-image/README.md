# Test Image

Status: scaffold

The initial Prompt 02 test boot image artifact is generated by:

- `zig build -p boot -fno-sanitize-thread -Drelease-safe test-image` from `boot/build.zig`

## TODO

- Replace placeholder binary with architecture-specific boot images.
- Add QEMU and hardware smoke boot instructions.
- Add integrity metadata and signature checks.
