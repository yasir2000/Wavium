package wavium:agent;

world ai-agent-component {
    export plan: func(input: string) -> string;
    export execute: func(input: string) -> string;
    export verify: func(input: string) -> string;
    export publish: func(input: string) -> string;
}
